class Main {
    static int DEBUG0, LED;

    method void init() {
        let DEBUG0 = 4107;
        let LED = 4096;
        return;
    }

    function void main() {
        var int x;
        do init();
        
        let DEBUG0[0] = 80; // implicit poke
        let x = DEBUG0[0]; // implicit peek
        
        if (x=80) {
            let LED[0] = 2; // 10
        } else {
            let LED[0] = 3; // 11
        }
        return;
    }
}
