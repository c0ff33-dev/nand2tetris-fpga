class Main{

    function void main() {
        while(true) {
            do GPIO.writeLed(GPIO.readLed()+1);
            // reduced to match slower CPU clock speed
            do Sys.wait(250);
        }
        return;
    }

}
