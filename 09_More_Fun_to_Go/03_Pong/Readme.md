# 03 Pong

The seminal game from the original nand2tetris course. It runs kind of slowly at current clock speed so disabled the remaining `Sys.wait()` calls.

Upload to real hardware with:

```
$ cd ~/src/nand2tetris-fpga/09_More_Fun_to_Go/03_Pong && make && make upload
```